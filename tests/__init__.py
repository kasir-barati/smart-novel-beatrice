"""Integration tests — hit the real GraphQL endpoint via HTTP.

Populate with real end-to-end tests as the endpoints land. Kept empty for now
so `make test-int` runs cleanly against the scaffold.
"""
